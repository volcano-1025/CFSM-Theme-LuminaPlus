# 纯静态部署（GitHub Pages 等）时使用，配合 npm run build:github-page。
# 由 Workers 托管主题时不需要这个文件。

# 必填：后端地址，多个用英文逗号分隔，只写 origin（不带路径与结尾斜杠）
API_BASE=https://status.example.com

# 选填：静态页面标题（Workers 托管时由后台外观设置注入，无需在此配置）
# TITLE=我的监控

# 选填：静态页面背景图
# BACKGROUND_IMAGE=https://example.com/bg.webp
