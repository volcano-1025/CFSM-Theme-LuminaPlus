# 更新日志

每个版本的第一行会被 CI 取出来，作为产物提交的说明 —— 也就是主题商店里显示的更新日志。
发新版时先改 `package.json` 的 `version`，再在这里加一段。

## v1.2.10

延迟/丢包柱子统一 20 格对齐后端新版；实时带宽不再虚高与乱跳

- **四种视图的延迟/丢包格子统一改成 20 格**：后端 2026-08-23 起改为从 D1 取一小时的
  ping/loss，返回条数由 30 条变成 20 条。格数和后端槽位必须一一对应，否则一格里会混进
  相邻槽位、或者反过来空出格子。原来四种视图各挑各的（大卡 30、迷你卡 24、小卡 18、列表 12），
  现在统一读 `HOMEPAGE_PING_BUCKET_COUNT`，一格正好 3 分钟、正好一个后端采样点，
  同一台节点在四种视图里的柱子终于落在同一条时间轴上。开页自检的格数（`HEALTH_BUCKET_COUNT`）
  一并跟着走，免得「空缺比例」的阈值飘。**跑旧版后端（还返回 30 条）也不会坏**：
  柱子是按时间聚合的，30 个点落进 20 格只是有的格子装两个点，不会错位。
- **修「最左边那格有时候空」**：后端一小时窗口的 20 行只跨 **57 分钟**（2026-08-23 实测
  8 台 56.7~56.9 分钟、步长 179~180 秒），而柱状图画的是 60 分钟 —— 最老那个样本本来就要
  向前补一段才铺得满。原来补的量取整个缓冲区采样间隔的**中位数**，而缓冲区末尾混着本地实测
  （约 60 秒一个），中位数被拉到 60 秒，补不够。于是只要后端最新一行还新（实测 age < 44 秒）
  第一格就是空的；age 在 3 分钟周期里从 0 涨到 180 秒，约四分之一的时间能看到，
  表现就是「有时候空、有时候不空」。现在改成按**这个样本自己到下一个样本的实际间隔**来补，
  上限不变。补的量仍有上限，所以「这台节点真的只有最近十分钟数据」时左边照样留空。

- **修小卡柱子换行**：上一条把格数从 18 改到 20 时漏了 CSS —— `.compact-node-health-bars`
  的 `grid-template-columns` 写死着 `repeat(18, ...)`，多出来的两根直接掉到第二排。
  现在改成 `grid-auto-flow: column` + `grid-auto-columns`，列数由柱子数量自己撑开，
  以后再改格数也不会重演（tooltip 是 absolute 的，不占轨道）。
- **适配后端的「输出首页详细 ping/loss」开关**（`sysConfig.show_three_net_details`）：
  关掉时后端不再下发 `servers[].ping[]` / `loss[]` 那一小时窗口，只剩每台节点当前的单条
  `ping_ct/cu/cm/bd`。主题据此回退两处 —— ① 三网那三条线不画，直接走单线路（否则画出三条
  空线）；② 开页自检不跑（窗口本来就不下发，柱子空是预期，不是数据坏了，再弹窗就是每次
  开页误报一遍）。**老后端不受影响**：没有这个字段时默认按 true 走，行为和以前一模一样。

- **底部加了署名**：每一页最下面一行 `Powered by CF-Server-Monitor · Theme by LuminaPlus`，
  两个都带 GitHub 链接（后端作者提的）。
- **首屏不再拿 `/api/servers` 快照里的瞬时速率**：那个接口有 30 秒服务端缓存，
  `net_in_speed` / `net_out_speed` 是被冻住的**某一个** 2 秒均值样本。2026-08-22 在线上取证：
  8 台节点的 `last_updated` 落后 29~78 秒，而速率字段**无一例外**高于其后 28 秒的 WS 均值
  （单台 1.35~10 倍，合计 1.0 MB/s 对 0.28 MB/s）。首屏照搬这份值，顶部「实时带宽」
  一打开就是几倍虚高，等第一帧 WS 到了再掉回来 —— 站长反馈的「刚打开/刷新完页面流量暴涨」就是它。
  现在快照超过 10 秒就不认它的速率，等 WS 的第一帧补上（实测连上后 1 秒内到齐）。
  **累计流量、在线状态照旧用快照**，那些字段不随时间衰减，旧一点也准；
  WS 确定连不上、靠轮询兜底时仍然采用快照速率 —— 那时它是唯一的数据源。
- **顶部「实时带宽」两次变化之间至少隔 1 秒**：上面那条挡掉快照速率之后，各节点的速率要等
  自己的第一帧 WS，而各节点的第一帧是**先后**到的（实测 8 台铺开在 1~2 秒里），求和会一路往上垒。
  垒的过程本身是照实的、保留 —— 有几台的数据就显示几台的和；问题是这段时间里它一秒内要跳好几次。
  根因在 `usePacedRate`：原来是 `setInterval` 加一句「paced 还是 0 就透传实时值」，而首屏整段
  爬升期间 paced 恰好一直是 0，于是每次渲染都直接透传，**1 秒节拍在最需要它的时候等于没生效**
  （聚合值真的是 0、即全部节点空闲时也会永久透传）。现在改成「相邻两次换值至少隔 1 秒」：
  第一个到的值立刻显示（等满一拍会让顶部干挂一秒的 0），之后来的新值攒住、到点一次性显示最新那份。
- **回放积压时跳到最新一帧**：原来每台节点每个出帧间隔只放一帧、队列上限 8 帧，
  标签页被后台节流一段时间后队列会攒满，切回来要十几秒才追上，期间还会把后台那会儿的
  旧尖峰当成「当前值」再播一遍。现在积压按当前节奏放完要超过 4 秒就只留最新一帧；
  正常节奏（积压 1~2 帧）仍然逐帧匀速回放，观感不变。判据用「还要放多久」而不是「积压几帧」，
  出帧间隔本来就大的慢节点才不会被误判成落后。
- **顺带查清楚、但不需要改的**：主题自身的流量消耗实测约 2.6 KB/s（WS 载荷 2.5 KB/s +
  `/api/servers` 每 60 秒一次），刷新页面命中缓存只有 6 KB，和内置主题一个量级；
  后端下发的 `net_in_speed` 逐帧核对与 `Δnet_rx ÷ Δts` 完全吻合，是准的；
  顶部求和的算法两个主题一模一样（在线节点速率直接相加，都不做平滑），
  所以**节点真有爆发流量时数字跳到 MB 级是照实画的**，不是 bug。

## v1.2.9

打开页面时自检延迟数据，柱子大片空缺就问要不要刷新（可关）；手机上能点柱子看数值了

- **自检**：页面首次打开、重开或刷新后（等 2.5 秒让首屏数据落定）检查一次 `/api/servers` 给的
  `ping[]` / `loss[]` 成色 —— 判据是**卡片上实际画出来的格子有多少是空的**（掉线的红格不算，
  那是照实画的）。非掉线格里空掉三成以上就算这台有问题，面板里有四分之一的节点中招才弹窗：
  刷新是所有节点一起付钱，为一台落单的节点打断用户不划算，那种情况留给右上角的刷新按钮。
- **弹窗把成本写在脸上**：写清楚这次要发几个请求、后端大约读多少行 D1（按
  `3600 ÷ 上报间隔` 逐台相加，节点增减会跟着变），两个选项 ——「刷新」立刻逐台拉一次真实采样，
  「跳过」什么都不做。默认焦点在「跳过」上，回车不会把请求打出去；Esc 或点空白处同样是跳过。
- **丢包全是 0 不算证据**：探测正常时丢包本来就该是 0。判定只看延迟柱子空不空，
  「后端返回了整段重复的同一个值」只作为弹窗里的**理由**说明（那些格子正是被 v1.2.8 的复印段
  过滤丢掉、才空出来的），不单独构成判据 —— 窗口再假，只要本地实测已经把这一小时铺满，
  卡片上就是真数据，这时候再花一次 D1 毫无意义。
- **不会反复打扰**：判定一次页面加载只跑一次（从详情页返回首页不会再问）；后端压根没探测过的
  节点不判（刷新也查不出行来）；掉线超过半小时的节点不判；30 分钟内刚刷新过也不问 ——
  那会儿的数据已经是花过 D1 换来的，还空着说明历史表里本来就没有。
- **自检本身一个请求都不发**：只看已经拿到的窗口和本地缓冲。**首页依旧不许自动查历史**，
  发请求的只有用户在弹窗里点的那一下。
- **可以关掉**：主题设置 →「主页延迟检测」→「延迟数据不完整时弹窗提醒」。关掉之后连自检都不跑，
  右上角的刷新按钮照常可用。
- **手机上能点柱子看数值了**：延迟/丢包那排格子原来只认鼠标悬停，而触屏根本没有悬停，
  于是手机上点了没有任何反应。现在按一下就出气泡（点哪儿是哪一格，手指按着横向划能连着看），
  松手后再留 2.5 秒；纵向滑动页面时不会误触。命中判定改成按整条宽度均分 —— 一根柱子连间距才
  4~5px，按柱子自己的范围算手指根本点不中；触屏上还把整条的可点区域上下各撑开 4px（占位不变）。
  列表视图整行是个链接，点柱条只看数值、不会顺手把详情页打开。

## v1.2.8

首页右上角新增手动刷新按钮，并不再绘制后端复制出来的延迟格子

- **手动刷新延迟数据**：右上角刷新按钮常驻（不用先展开快捷栏），点一下等同于「把每台节点的
  详情页都点开一遍」—— 逐台拉一次 `hours=1` 的历史，回灌首页延迟条的缓冲区，并发上限 4 台。
  刚打开面板时不必再干等页面开满一小时，柱子立刻就是真实采样。
- **为什么需要它**：`/api/servers` 那个一小时窗口是后端**复制最近一个样本**填出来的 ——
  后端源码里 `buildFixedLatencySeries` 固定输出 30 格、每格取「距离最近的样本」且没有距离上限，
  而桶只在有人开着面板时才攒。于是「30 个点里有几个是真的」等于有人看着的分钟数 ÷ 2：
  刚打开时 1 个真值 + 29 份复印（柱子是一面纹丝不动的墙），一小时没人看则整段被剪光
  （柱子只剩最右边一格）。这两种截图都在线上复现过。
- **30 分钟内重复刷新会先提醒**：距上次刷新不到 30 分钟就再点，按钮转成琥珀色并提示
  「N 分钟前才刷新过，数据没长多少 · 再点一次仍会刷新」，**这一次不发任何请求**；确实想要就在
  警告条还挂着的时候（15 秒）再点一下，照刷不误。是提醒不是禁止。上次刷新时间存 localStorage，
  按 F5 绕不过去。
- **刷新有完成提示**：刷完按钮短暂变成绿色对勾，下方弹出「延迟数据已更新 · N 台」，几秒后自动收起；
  失败变红并提示重试，部分失败会带上失败台数。之后悬停按钮还能看到上次刷新时间。
- **不再画后端编出来的格子**：`/api/servers` 窗口里连续 4 格以上「四条线路的延迟和丢包同时
  逐字节相同」的段落一律判为复印件丢掉，那些格子留空。一台刚加进来 7 分钟的节点，窗口里也会有
  整整一小时的「数据」（线上实测 Uzumaru-tw：30 格全是 `1/1/1`，而历史表里只有 13 行、跨度
  6.3 分钟）—— 现在它只画真实覆盖到的那几分钟。留空比画编出来的数据诚实，丢包加权平均也不会
  再被这些格子带偏。
- **首页仍然不许自动查历史**：只有用户点击这一个入口。一台节点一小时的行数 = `3600 ÷ 上报间隔`，
  点一次是各节点之和（线上 8 台、30/60 秒上报约 900 行）；每分钟自动拉一次是它的 60 倍 ——
  禁的是后者。后端对 1 小时档有 60 秒服务端缓存，连点不会真的重复读库。

## v1.2.7

首页延迟/丢包改以浏览器实测为准，修正与详情页对不上的问题

- **首页数据以浏览器实测为准，后端窗口只补没覆盖到的时段**：`/api/servers` 给的一小时窗口
  并不是把探测结果聚合出来的，而是**拿最近一次结果向后填充**。线上实测某节点的窗口里，前 24 格
  （48 分钟）是同一个数字重复，只有最后 6 格是真值；同一时段详情页历史有 114 行真实采样，
  延迟 235~1283ms 都有、丢包 5.5%，按窗口算却只有 1.1% —— 这就是「首页和详情页差很大」的来源。
  现在浏览器自己从 WebSocket 攒到的实测值优先，窗口只用来顶上页面打开之前、或标签页被挂起的
  那段时间。
- **修正丢包率的采样与计权，误差从 +54% 降到 −7%**（构造场景：60 次探测含 8 次丢包）：
  - 采样改成「一次探测记一个样本」。探针 60 秒才测一次、WS 每 2 秒把同一个结果重推一遍，
    早先「值没变 50 秒记一个、值变了 20 秒就放行」的不对称阈值会在每次丢包的**切换处**多记一个
    样本，丢包样本被系统性多记。现在值变了就记，值没变每 2 分钟留一个心跳保证柱子画得出来。
  - 每个样本按**它代表的时长**计权，而不是一条算一条 —— 否则疏密不同的两个来源里，密的那段
    说话大好几倍，丢包率还会随页面开着的时长漂移。时长按「到下一次采样为止」算（与图表铺格子
    一致），按「前后邻居的中点」算会让只代表 60 秒的丢包样本拿到 90 秒权重。
- **详情页看过的节点，首页改用真实历史**：点开详情页时本来就查了那台节点的历史，顺手把这一小时的
  真实采样灌进首页缓冲 —— 不额外打后端（首页自己仍然不查历史），但看过的节点，首页那一小时就不必
  再靠那份向后填充的窗口凑。
- **首页柱子从 24 格改成 30 格**：与后端一小时窗口的 30 个槽位一一对应。
- **窗口里没值的那一格用本地实测点顶替**：后端某轮探测没出结果时那一格是「格子在、值是 null」，
  图表对这种槽位是真的留空的 —— 附近有实测点就让它顶上，没有才留空。
- **卡片上那个「当前延迟」数字取 WebSocket 的实时值**，柱子和丢包率仍是一段时间的聚合。
- **丢包 tooltip 不再报「N 次采样」**：样本数现在是加权值，报出来只会让人算不明白。
- **全量刷新 30 秒改成 60 秒**：`/api/servers` 有服务端缓存（站长口径 30 秒；实测同一份字节
  至少冻结 24 秒，加随机参数也绕不过），30 秒一拉多半只是拿回同一份数据。
- **修复：后台用英文逗号分隔的标签没被拆开**。后台「标签」输入框的提示是「英文逗号割开」，
  而主题从 Komari 移植过来时只按分号拆，于是 `边缘,高带宽` 整条变成了一个标签。现在逗号、
  分号、全角 `，；` 都认。
- 开发用：`npm run dev` 现在会读 `.env` 里的 `API_BASE` 并写进 `<meta name="apiBase">`，
  本地调试可以直接连线上后端（后端需把 dev 地址加进跨域白名单）。感谢 @huilang-me（#1）。

## v1.2.6

实时刷新改为按各节点自己的上报节奏匀速更新

- **每台节点按自己的速率匀速刷新**：实测各节点上报快慢不一，且有的节点会一次上报多帧。
  现在按节点实测「平均多久到一帧」，据此定一个**固定**出帧间隔，并把下一帧对齐到网格
  （从上次应放的时刻推进一个间隔，而不是从「现在」重算）—— 2 秒来 2 帧就恒定 1 秒一帧、
  6 秒来 3 帧就恒定 2 秒一帧、2 秒来 1 帧就 2 秒一帧，快的放得密、慢的放得疏，各自都匀速。
  按队列深度临时换算会走成 1000→2000→1000→667 的循环（队列放空后干等一整个到达间隔、
  下一批又挤在一起），正是「一次上报多帧的节点显示不匀」的原因。
- **顶部「实时带宽」按 1 秒节拍**：它是跨节点求和，各节点相位不同会让它一秒内变好几次、
  读数还没看清就跳了。现在统一按 1 秒换一次；卡片仍按各自节奏走。

## v1.2.5

修复显示、流量统计与实时刷新的几处问题

- **延迟 / 丢包柱中间缺一大段（刚打开尤其明显）**：后端 `/api/servers` 的探测窗口会间歇性
  停止滑动 —— 线上实测到 7 台节点一致：2 分钟网格铺到约 35 分钟前就停了，末尾直接追加一个
  「当前」点，中间是空洞（过一阵后端自己追上）。主题原先拿窗口整体替换本地缓冲、只保留比窗口
  末点更新的样本；而末点约等于「现在」，等于把浏览器攒下并已持久化的那段全判成旧数据丢掉，
  于是空洞既显示出来、又被写回本地存储。现在改为按时间戳取并集（同一时刻以后端为准），
  本地样本正好填上窗口的空洞。缓冲区超出条数上限时按时间**均匀抽稀**而不是砍掉最老的 ——
  WSS 下本地约 20 秒攒一个、很快顶满上限却只覆盖最近半小时，直接砍最老的会把窗口里较早的点
  整段丢掉，柱子于是左半段空、右半段有。
- **柱子首帧绘制**：canvas 原本只等 ResizeObserver / IntersectionObserver 的异步回调，
  首屏繁忙时会留下一帧空白。现在挂载时同步补一次宽度与可见性测量，在屏卡片首帧即绘制。
- **「实时带宽」总览不再把掉线节点的旧速率算进去**：节点掉线后 `/api/servers` 仍会沿用
  最后一个快照，之前会一直把那台死节点最后一刻的带宽计入总量、带宽评级与排序。现在掉线即按
  0 计，与每台节点的实时流量迷你图口径一致（累计流量是生命周期总量，不受影响）。
- **「今日流量」估算更稳、不再动辄跳 100 倍**：它是用历史瞬时速率积分出来的估算值（后端没有
  今日累计字节字段）。改成梯形积分（相邻两点速率取平均）去掉系统性高估，并对单点离群速率按
  当日中位数的倍数钳制，避免后端偶发返回的高速毛刺把总量带飞；峰值速度仍报真实观测值。
  今日流量旁加「估算」角标，提示这是估算、会随采样波动。
- **「实时带宽」总览不再乱跳**：各节点错开上报、每次提交只更新其中几台，跨节点求和若跟着每次
  提交重算，就是「部分新 + 部分旧」拼出来的和。现在聚合值按固定节拍整体换一次，匀速前进。
- **移除「今日流量」与「峰值速度」**：后端没有今日累计字节字段，两者只能由历史瞬时速率积分 /
  取极值得出，而历史接口每次固定只返回约 120 个点（区间越长采样越粗），数值会随返回的采样点
  大幅漂移，误导性大于参考价值 —— 详情页两行与首页卡片的今日流量悬浮窗一并移除（连同其设置项）。
- **隐藏流量统计页**：`#/traffic` 整页与首页「累计流量」卡的入口一并移除 —— 它展示的同样是
  今日流量 / 采样峰值这类估算值。
- **修复实时带宽「一进去很快、然后慢慢掉下来」**：`/api/servers` 是缓存快照，`last_updated`
  常比 WS 推来的样本旧十几到几十秒，其中的瞬时速率又明显偏高（线上实测个别节点 REST 24KB/s
  对 WS 0.8KB/s，合计约 4 倍）。每 30 秒一次的全量刷新照单全收，就会拿这份旧值盖掉新鲜的 WS
  实时值，带宽于是每半分钟被重新抬高一次再慢慢掉回去。现在快照不比现值新时只取 WS 不下发的
  字段（月度累计等），实时部分保持现值。
- **Ping 图表标出采样分辨率**：标题下显示「每 N 分钟一个采样点」。查询超过 1 小时时，后端按
  后台设置的采样点数返回（「查询超过 1 小时时返回的采样点数」，可选 60/120/180/240），点数固定
  而区间不固定，于是区间越长采样越粗；持续一两分钟的短促丢包在长区间可能整段没被采到 ——
  同一次丢包「12 小时看得见、1 天看不见」正是此因（调大后台采样点数可缓解），色带开关的说明
  里也写清了。
- **适配后端 WSS 上报：实时刷新改为匀速**：后端会按「有没有人在看」调整上报频率，一次推来的
  样本数并不固定；主题原本来一条就渲一次，于是几十毫秒内连刷好几帧、然后静默数秒（「一顿一顿」），
  慢节点补发的多帧还会回放旧点（刚打开「乱跳」）。现在按**到达间隔 ÷ 待放帧数**把队列匀速铺开：
  一批来 1 帧就按到达节奏更新，一批来 2 帧就间隔减半、在下一批到达前正好放完 —— 不论后端怎么调
  频率，观感都是匀速。节奏取自实际到达时间而非样本 ts（补发的历史帧 ts 相隔十几秒却同时到达），
  队列深度参与分母，积压时自动加快、追平后回落。线上实测：到达间隔 2002ms，放帧间隔稳定 ~1000ms、
  无积压。

## v1.2.4

优化卡片显示

- **节点掉线后，卡片的延迟 / 丢包柱逐格转红**：以前掉线段会被最后一个已知值一路填满，
  看不出节点已经没了。现在按节点最后一次上报截断，之后的格子涂红，每 2.5 分钟往左多红一格，
  一小时后整条全红；悬浮提示写「离线」，与「这一格没采到样本」区分开。
  掉线期间后端窗口若仍在沿用旧值往前铺格子，这些样本一律不采信。
- **Ping 图表的辅助线支持触屏拖动**：原来只绑了鼠标事件，手机上辅助线拖不动。
  现在横向拖动移动游标、纵向滑动照常滚页面，抬手后数值停留在原处方便读。负载图表同样生效。
- **Ping 图表 tooltip 改成「丢包在前、延迟在后」**：数值列是右对齐的，
  延迟固定放末尾后，有丢包和没丢包的行 ms 才落在同一列上。

## v1.2.3

Ping 图表新增丢包色带

- **Ping 图上方新增丢包色带**：每条线路一行，横轴与折线严格对齐，越红丢得越多，
  空缺表示该时段没有采样（掉线和「丢包 0%」看得出区别）。悬浮时游标虚线贯穿色带与折线，
  tooltip 同时给出延迟和丢包率。工具栏有开关，默认开启。丢包按样本数加权平均，不受「削峰平滑」影响 ——
  否则一次 100% 的采样会把整格染红。数据来自原有的历史请求，后端零额外开销。
- **Ping 图纵轴恒定从 0 开始**：原来会截取数据所在的那一段（比如 200–400ms），
  波动看着惊心动魄，其实只差几十毫秒。
- **修正纵轴刻度被截断**：延迟冲到四位数时 `1400 ms` 放不下轴宽，被裁成 `400 ms`，
  读数直接错一个数量级。轴宽从 54px 放到 64px。
- **Ping 图默认档位从 6 小时改成 1 小时**：详情页每打开一次就是一趟全量历史查询，
  默认档位越短后端读的行越少；需要更长区间照旧手动切。
- **设置页「恢复站点默认」改名为「同步后台配置」**：这个按钮做的一直是「丢掉本机设置、
  改用后台最新的配置 JSON」，但旧名字听起来像恢复出厂设置，没人会想到它就是拉取后台配置的
  入口。按钮说明也一并写清楚，并点明会连配色一起清掉。
- 图表开关不再重复显示「开启 / 关闭」文字，状态看拨钮即可。
- 工具栏顺序调整为：重置 · 复制配置 JSON · 同步后台配置 · 保存设置。

## v1.2.2

默认开启三网模式

- **默认开启三网模式**：新站点装上就是电信 / 联通 / 移动三行延迟，不用再进设置页配。
  已经显式关掉三网的站点不受影响。
- **延迟线路卡片精简**：去掉 `ICMP`、`60s`、`ID` 三个标签和「未填写目标」——
  探测方式与目标都在后台的服务器编辑里配置，公开接口不下发，主题显示的是写死的值，
  站长照着它判断会被误导。绑定说明改成「N 台节点在首页显示这条线路的延迟」，
  电信卡片标出「默认线路」，空线路直接提示去哪里加节点。
- **实例信息页「虚拟化」换成「内核版本」**：后端不上报虚拟化类型，那行永远是「—」；
  内核版本是后端一直下发、主题却没用上的字段。
- **负载图表的「磁盘」改画磁盘 IO**：读取（紫）/ 写入（黄）两条线（MB/s），已用空间挪到副标题。
  实时档取 `/api/servers` 的 `disk` 对象，历史档取历史行里的 IO 字段；探针没上报 IO 时
  整张卡片退回原来的已用空间图。

## v1.2.1

修复「复制配置 JSON」漏掉卡片配色

- 导出快照时会先按白名单归一化，取色器调的卡片配色与黑色程度不在白名单里，于是被整段丢掉，
  站长粘到后台后配色仍是默认值。现在这两项按「站点预设 + 本机覆盖」的口径一起导出，
  没改过就不写进 JSON。

## v1.2.0

设置页新增「复制配置 JSON」

- **复制配置 JSON**：第三方主题不能写后端设置，多设备同步只能靠后台的「主题自定义配置」。
  这个按钮把当前设置导出成完整快照（含配色等设置页之外的项），一键复制、粘贴保存即可
  成为所有设备与访客的默认值；某台设备若已有本地覆盖，点「恢复站点默认」就会跟随。
  纯 http 部署没有 `navigator.clipboard`，已带 `execCommand` 兜底。
- **卡片显示价格**：首页巡检里新增开关，关掉后大卡片内不再显示价格（其他卡片档位不受影响）。
- **小卡片占位文案**：没填到期日期显示「永久」（原来是「余 --」），没填价格显示「免费」
  （原来是「未填」）。
- 设置页顶部去掉「主题 CFSM-Theme-LuminaPlus」一行，说明文字改为提示如何做多设备同步。

## v1.1.2

修复资产统计：三年付被当成年付、汇率拿不到时整卡显示「—」

- **账单周期**：后端下拉的 `half_year` / `two_years` … `five_years` 之前一个都认不出来，
  统统退回年付 —— 三年付显示成「年」，月均和剩余价值虚高三倍。现在按真实周期摊销。
- **汇率失败不再清空资产卡**：人民币定价的节点根本不需要换算，照样统计，
  只有外币节点标为「汇率缺失」；卡片下方会注明「仅统计人民币」。
  之前只要汇率接口失败——或者浏览器被判定离线让请求一直挂起——整张卡就只剩一个「—」。
- **`price = -1`**：后端用它表示「免费」，之前被归零、卡片上什么都不显示，现在显示「免费」。

## v1.1.1

卡片上的丢包率改回原版口径：最近一小时的平均，而不是最后一次采样

移植时把原版的区间平均写成了取最新一个样本，于是「6 个包丢 1 个」这种单次抖动会让数字
直接跳到 16%，而下面代表整小时的柱子还是全绿；反过来最后一次采样正常时，即使一小时里
有 33% / 66% 的丢包段，数字也显示 0%。现在数字与柱子同源。延迟数字仍然是最近一次采样。

## v1.1.0

修复首页延迟柱状图缺格、丢包率只显示 0% 或 100% 的问题

- 延迟柱状图：每个采样点延续到下一次采样为止（最长 5 分钟），补上后端窗口最新一格
  不在 2 分钟网格上造成的空缺；最老的一格也补齐，最左边不再空一格。
  窗口里明确没有测到值的槽位仍然如实留空。
- 丢包率：后端给的是百分比，之前换算成整数包数再求比例，33% 被抹成 0%、50% 变成 100%。
  现在保留小数，柱子颜色能反映真实丢包。
- 产物 `index.html` 写入 `<meta name="theme-version">`，方便确认线上跑的是哪一版。

## v1.0.0

首个版本：由 Komari 主题 LuminaPlus 移植到 CF-Server-Monitor
